# Claude Code Minimal Example

> 🔵 **Claude Code ONLY** — Kimi users see `../kimi-codex-mcp/examples/kimi.md`

Use native Codex MCP and switch provider/model in the tool call.

```text
Use the codex MCP tool.
Set `model` to `google/gemini-3-flash-preview`.
Set `config.model_provider` to `zenmux`.
Review the current changes and return findings first.
```
